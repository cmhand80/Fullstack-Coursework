const name = "User";
let str1 = "Hello " + name + ". You have been chosen to open an important vault. Here is the secret combination:";
// Assigned gretting for user on page
let numLock1 = 2 * 5;
let numLock2 = 5 * 8;
let numLock3 = 30 + 9;
// Assigned combination, used arithmetic operators to calculate combinations for User
alert(str1 + ' ' + numLock1 + ' ' + numLock2 + ' ' + numLock3);
// Alerted User with greeting and combination